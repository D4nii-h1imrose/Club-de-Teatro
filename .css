body {
      font-family:system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      background: linear-gradient(135deg, #2c3f6e, #85298d); /* Degradado azul a rojo */
      min-height: 100vh;
      text-align: center;
      margin: 0;
      padding: 20px;
      color: #ffffff;
    }

    h1 {
      color: #f6faff;
      text-shadow: 3px 3px 9px rgba(0,0,0,0.4);
    }

    .botones {
      margin-bottom: 20px;
    }

    button {
      background: rgba(255, 255, 255, 0.2);
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.4);
      padding: 10px 20px;
      margin: 5px;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s;
      backdrop-filter: blur(4px);
    }

    button:hover {
      background: rgba(255, 255, 255, 0.4);
      color: #000;
    }

    .info {
      display: none;
      background: rgba(255, 255, 255, 0.9);
      color: #111;
      border-radius: 10px;
      padding: 20px;
      margin-top: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.2);
      text-align: left;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }